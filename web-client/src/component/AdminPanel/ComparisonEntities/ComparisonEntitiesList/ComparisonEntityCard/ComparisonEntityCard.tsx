import React from 'react';
import styles from './ComparisonEntityCard.module.less';

const ComparisonEntityCard: React.FC = () => {

    return (
        <div>
            Card
        </div>
    );
};

export default React.memo(ComparisonEntityCard);
